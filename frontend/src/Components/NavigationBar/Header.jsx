import Menu from "./Menu";
import AppBarResponsive from "./AppBarResponsive";

const Header = (props) => {
  return (
    <header>
      <AppBarResponsive />
    </header>
  );
};

export default Header;
